defaults:
  - _self_
  - datamodule: mask_mm.yaml
  - module : alise_mm.yaml
  - train: pretrain_ssl.yaml
  - callbacks: minimal.yaml
  - logger: default.yaml
  - dataset: mask_mm.yaml
hydra:
  run:
    dir: ${path_run_dir}/true_requeue
  sweep:
    dir: ${path_export_dir}${hydra.job.name}/requeue
    subdir: ${hydra.job.override_dirname}
  job:
    name: mm_mae_sd
path_run_dir: ${path_export_dir}/${hydra.job.name}/wrec${train.w_rec}_winv${train.w_inv}_wcr${train.w_crossrec}/bs${train.batch_size}/seed${seed}
load_model: false
path_dir_model: null
dir_training: null
seed: 1
path_export_dir: /home/ad/dumeuri/trainings/mm_mae_trainings
verbose: 0 